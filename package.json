{
  "name": "passmark-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.2.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.6"
  }
}
